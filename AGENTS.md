# Shared editing instructions
The website is the React + TypeScript + Tailwind app in web/ (see README.md); edit it there and build with `npm --prefix web run build`. Read README.md first. Keep Gujarati accents, accessible keyboard controls and the rust/gold identity. Animations and mandala loops stay on for every device, including when the system asks for reduced motion (the organiser's decision). Keep event settings in web/src/site/content.ts. Do not invent pass prices, the lineup or a WhatsApp number. Do not inject any Claude artifact or ChatGPT runtime code. Never commit tokens. Both Claude and Codex must edit the same checkout to share changes.
